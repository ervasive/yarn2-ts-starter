{
  "name": "fullstack",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "ts:compile": "tsc --build",
    "ts:clean": "tsc --build --clean",
    "ts:watch": "tsc --build --watch"
  },
  "devDependencies": {
    "@types/node": "^14.14.20",
    "@types/prettier": "^2",
    "prettier": "2.2.1",
    "typescript": "^4.1.3"
  }
}
